# Core data & ML
pandas>=2.0,<3.0
numpy>=1.24,<3.0  # widened from <2.0 to support Python 3.14 (numpy 2.x has wheels; 1.x doesn't)
scikit-learn>=1.7.2,<2.0
opencv-python>=4.8  # cv2 — image processing used by api/src
python-dotenv>=1.0  # dotenv
requests>=2.31
prometheus-fastapi-instrumentator>=8.1,<9.0  # /metrics instrumentation in api/main.py; 8.1+ required for fastapi>=0.116 router handling
joblib>=1.5.3,<2.0
scipy>=1.10,<2.0

# Pydantic settings
pydantic>=2.0,<3.0
pydantic-settings>=2.0,<3.0

# Fuzzy joining
rapidfuzz>=3.14.5,<4.0

# NLP
nltk>=3.8,<4.0

# Network analysis
networkx>=3.1,<4.0

# Plotting / visualisation
matplotlib>=3.10.9,<4.0
plotly>=5.17,<7.0  # widened from <6.0 for Python 3.14 wheel compatibility
kaleido==0.2.1  # pinned: 1.2.0+ incompatible with plotly 5.x; 0.2.1 works with 5.x and 6.x

# Optional ML (install together with extras):
# xgboost>=2.0,<3.0
# lightgbm>=4.0,<5.0
# catboost>=1.2,<2.0
# shap>=0.42,<1.0

# Streamlit dashboard
streamlit>=1.62.0,<2.0
streamlit-option-menu>=0.4.0,<1.0

# FastAPI
fastapi>=0.116,<1.0  # floor 0.116: instrumentator 8.x routing fix requires it
uvicorn>=0.52.4,<1.0
pydantic>=2.0,<3.0
structlog>=26.1.0,<27.0
slowapi>=0.1.10,<1.0
tenacity>=9.1.4,<10.0
diskcache>=5.6.3,<6.0
httpx>=0.24,<1.0

# Testing
pytest>=7.4,<8.0
pytest-cov>=4.1,<5.0
httpx>=0.24,<1.0
hypothesis>=6.165.10,<7.0

# Dev
pre-commit>=4.6.2,<5.0
black>=26.5.1,<27.0
isort>=5.12,<6.0
ruff>=0.16.4,<1.0

# Docker / env
python-dotenv>=1.0,<2.0
requests>=2.28,<3.0
Pillow>=12.3.0,<13.0  # widened for Python 3.14 wheel compatibility (12.x is current)
opencv-python>=4.8,<5.0
gunicorn>=26.1.0,<27.0

# OpenTelemetry distributed tracing (optional, enabled via OTEL_ENABLED=true)
opentelemetry-api>=1.20.0,<2.0
opentelemetry-sdk>=1.20.0,<2.0
opentelemetry-exporter-otlp-proto-grpc>=1.20.0,<2.0
opentelemetry-instrumentation-fastapi>=0.41b0,<1.0
